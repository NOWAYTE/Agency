// tailwind.config.js
module.exports = {
  darkMode: 'class', // or 'media' if you prefer automatic based on system
  theme: {
    extend: {
      fontFamily: {
        sans: ['var(--font-sans)'],
        mono: ['var(--font-mono)'],
      },
    },
  },
};
